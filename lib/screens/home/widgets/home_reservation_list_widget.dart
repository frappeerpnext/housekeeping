import 'package:flutter/material.dart';

class HomeReservationListWidget extends StatelessWidget {
  const HomeReservationListWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Reservation List'),
      ),
      body: Center(
        child: Text("data"),
      ),
    );
  }
}
