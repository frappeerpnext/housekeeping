import 'package:flutter/material.dart';

class ReservationCancelListScreen extends StatelessWidget {
  const ReservationCancelListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Reservation Cancel"),
        centerTitle: true,
      ),
    );
  }
}
